{
  "buildCommand": "pnpm install && pnpm turbo build --filter=@beetlesense/web",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
