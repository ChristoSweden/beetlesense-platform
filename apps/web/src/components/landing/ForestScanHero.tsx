/**
 * ForestScanHero — now redirects to HeroSection.
 * The old Three.js 3D forest hero has been replaced with the
 * premium command-center HeroSection.
 */
import { HeroSection } from './HeroSection';

export default function ForestScanHero() {
  return <HeroSection />;
}
